# 进度记录（AI Tarot 项目）

## 今日推进
- 事件抽牌流程改为“先抽牌、不可重复抽、可重置再抽”，成功保存后自动清空事件表单。
- Paywall/会员对比弹窗落地，配额耗尽/降级限制会触发升级提示与兑换入口。
- Topic 列表/详情带事件计数与剩余额度显示；事件 AI 提示含基准牌与历史上下文。

## 待办/风险
- 事件抽牌仍为随机单张抽取，可考虑加入视觉翻牌动画、允许在生成前重新抽（需同时更新“不可重抽”规则决策）。
- 列表侧配额为估算（基于 event_count），可进一步从 API 返回精确 per-topic 余量。
- 路由仍为单页/弹窗，若需分享/刷新保留上下文，可引入简易路由。
